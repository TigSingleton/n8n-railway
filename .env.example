# N8N Configuration
# -----------------
# Basic Auth (for logging into n8n dashboard)
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=

# General
GENERIC_TIMEZONE=Asia/Manila
N8N_ENCRYPTION_KEY=

# Supabase Connection (PostgreSQL)
# ------------------------------
# Get these from Supabase Dashboard > Project Settings > Database > Connection Parameters
# Be sure to use "Transaction Mode" port if available (6543) or Session Mode (5432)
DB_POSTGRESDB_HOST=aws-0-us-east-1.pooler.supabase.com
DB_POSTGRESDB_PORT=6543
DB_POSTGRESDB_DATABASE=postgres
DB_POSTGRESDB_USER=postgres.[project-ref]
DB_POSTGRESDB_PASSWORD=[your-database-password]

# Note: Railway will provide RAILWAY_PUBLIC_DOMAIN automatically.
